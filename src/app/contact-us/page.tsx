'use client';

import { Contacts, FeedbackHint } from '@/widgets/contact-us/components';

export default function ContactUs() {
  return (
    <main>
      <Contacts />
      <FeedbackHint />
    </main>
  );
}
